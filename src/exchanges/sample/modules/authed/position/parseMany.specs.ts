import { expect } from 'chai'

import { parseMany } from './parseMany'



describe(__filename, () => {

  it('should parse many positions just fine', async () => {
    expect(parseMany).to.exist
  })

})
