export enum HuobiOrderStatusEnum {
  CREATED = 'created',
  SUBMITTED = 'submitted',
  PARTIAL_FILLED = 'partial-filled',
  FILLED = 'filled',
  CANCELED = 'canceled',
  TRIGGERED = 'triggered',
  REJECTED = 'rejected',
}
